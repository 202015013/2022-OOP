/*import java.util.*;
public class LoadingPolynomial extends CommandManager {
    boolean checkPolyName(Scanner sc) {
        if (find(sc) == null) {// 이름의 다항식이 없으면 오류
            sc.nextLine();  //  이름 오류이므로 뒤 부분 읽어 버림
            return false;
        }
        return true;
    }
}

*/